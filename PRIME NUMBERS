#include <stdio.h>

int main()
{
    int a;
    
    printf("enter the value of (a)");
    scanf("%d",a);
    if("a%2=1");
    {
        printf("the value is prime");
    else
    
        printf("the value is not prime");
    }
    
    
}
